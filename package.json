{
  "name": "claude-redlining",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n flask,next -c blue,green \"cross-env PORT=5002 python run.py\" \"npm run dev --prefix frontend\"",
    "dev:flask": "cross-env PORT=5002 python run.py",
    "dev:next": "npm run dev --prefix frontend",
    "kill": "lsof -ti:3050,5002 | xargs -r kill -9; rm -f frontend/.next/dev/lock",
    "restart": "npm run kill && npm run dev",
    "build": "npm run build --prefix frontend",
    "test": "echo \"See pytest for backend tests\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^10.1.0"
  }
}
