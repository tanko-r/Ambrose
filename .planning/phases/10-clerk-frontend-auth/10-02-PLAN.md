---
phase: 10-clerk-frontend-auth
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - frontend/src/components/layout/header.tsx
autonomous: false
requirements: [AUTH-07, PROT-02]

must_haves:
  truths:
    - "User sees their avatar in the header (replaces generic user icon)"
    - "User can sign out from the header avatar menu"
    - "User can access account settings (profile, MFA) from the header avatar menu"
    - "Header no longer shows placeholder 'Profile' and 'Logout' menu items"
  artifacts:
    - path: "frontend/src/components/layout/header.tsx"
      provides: "Clerk UserButton replacing placeholder user dropdown"
      contains: "UserButton"
  key_links:
    - from: "frontend/src/components/layout/header.tsx"
      to: "@clerk/nextjs"
      via: "UserButton + SignedIn imports"
      pattern: "import.*UserButton.*from.*@clerk/nextjs"
---

<objective>
Replace the placeholder user dropdown in the header with Clerk's UserButton component, and verify the complete auth flow end-to-end.

Purpose: Wires user identity into the app UI — avatar display, sign-out, account management (including MFA setup). This completes the frontend auth integration.

Output: Header shows Clerk UserButton with sign-out and account management. Full auth flow verified.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-clerk-frontend-auth/10-RESEARCH.md
@.planning/phases/10-clerk-frontend-auth/10-01-SUMMARY.md
@frontend/src/components/layout/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace header user dropdown with Clerk UserButton</name>
  <files>frontend/src/components/layout/header.tsx</files>
  <action>
1. In `frontend/src/components/layout/header.tsx`:
   - Add imports: `import { UserButton, SignedIn } from '@clerk/nextjs'`
   - Remove unused imports that were only for the user dropdown: `User`, `UserCircle`, `LogOut` from lucide-react (keep any still used elsewhere in the component)
   - Remove the second `DropdownMenu` block (the user menu at lines ~147-169) — the one triggered by the User icon button with "Profile", "Settings", "Logout" items
   - Replace it with:
     ```tsx
     <SignedIn>
       <UserButton
         showName={false}
         userProfileMode="modal"
         appearance={{
           elements: {
             avatarBox: 'h-8 w-8',
           },
         }}
       />
     </SignedIn>
     ```
   - Keep the theme toggle button and "New" button exactly as they are
   - Keep the Settings menu item in the hamburger menu (left-side DropdownMenu) — only remove the user dropdown on the right

2. Clean up unused imports — after removing the user dropdown, check which lucide-react icons and dropdown components are still used. The hamburger menu still uses DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuTrigger, Menu, Plus, FolderOpen, FileText, Settings, HelpCircle. The `SlidersHorizontal` import can be removed (was only in user dropdown's Settings item). `User`, `UserCircle`, and `LogOut` can be removed if not used elsewhere.

IMPORTANT: Do NOT remove the `DropdownMenu` imports — the hamburger menu (left side) still uses them. Only remove the user-dropdown-specific icons.
IMPORTANT: The `<UserButton />` component handles sign-out, profile editing, and MFA management automatically via its built-in modal. No custom code needed for AUTH-07 (sign-out) or AUTH-06 (MFA settings).
  </action>
  <verify>
    Run `cd frontend && npx tsc --noEmit` — no type errors. Run `cd frontend && npm run build` — build passes. Grep for "UserButton" in header.tsx to confirm it's present. Grep for "LogOut" in header.tsx to confirm the old placeholder is removed.
  </verify>
  <done>
    Header renders Clerk's UserButton (avatar, sign-out, account management modal) instead of the placeholder user dropdown. No unused imports remain. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Clerk auth flow end-to-end</name>
  <action>
Human verifies the complete Clerk authentication integration built across plans 10-01 and 10-02. This covers route protection, sign-in/sign-up, OAuth, email verification, session persistence, UserButton in header, sign-out, and account management.
  </action>
  <what-built>
Complete Clerk authentication integration:
- Route protection middleware (proxy.ts) redirects unauthenticated users to /sign-in
- Sign-in page with Clerk's prebuilt component (email/password, OAuth buttons, password reset)
- ClerkProvider wrapping the app with shadcn theme
- Header UserButton replacing placeholder user dropdown (avatar, sign-out, account management with MFA)
  </what-built>
  <how-to-verify>
**Prerequisites:** You must have configured Clerk Dashboard and added real API keys to `frontend/.env.local` (replacing the pk_test_REPLACE_ME / sk_test_REPLACE_ME placeholders).

1. Start the dev server: `cd frontend && npm run dev`
2. Visit http://localhost:3000 — you should be redirected to /sign-in
3. On the sign-in page, verify:
   - Email/password fields are visible
   - Google OAuth button is visible (if enabled in Clerk Dashboard)
   - Microsoft OAuth button is visible (if enabled in Clerk Dashboard)
   - "Don't have an account? Sign up" link is present
4. Create an account with email + password
5. Verify you receive an email verification (if enabled in Dashboard)
6. After verifying, confirm you land on the main app page
7. In the header (top right), verify:
   - Your avatar/initials appear (Clerk UserButton)
   - No more generic user icon or "Profile"/"Logout" placeholder items
8. Click the avatar — verify the popover shows:
   - "Manage account" option (opens profile modal with MFA settings)
   - "Sign out" option
9. Click "Sign out" — verify you're redirected to /sign-in
10. Close and reopen the browser, visit http://localhost:3000 — verify session persists (you're still logged in, or redirected to sign-in if session expired)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Header shows Clerk UserButton instead of placeholder user dropdown
- Sign-in page handles email/password, OAuth, password reset, email verification
- Unauthenticated users are always redirected to /sign-in
- Sign-out from UserButton redirects to /sign-in
- Account management modal accessible from UserButton (includes MFA if enabled)
- No TypeScript errors, build passes
</verification>

<success_criteria>
- Complete auth flow works: sign-up -> email verify -> sign-in -> use app -> sign-out
- OAuth buttons visible on sign-in page (Google + Microsoft)
- UserButton in header replaces all placeholder user menu items
- Session survives browser close/reopen
</success_criteria>

<output>
After completion, create `.planning/phases/10-clerk-frontend-auth/10-02-SUMMARY.md`
</output>
