---
phase: 08.1-documentation-sync-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Transmittal dialog opens from bottom bar and shows email content with flagged items"
    - "Transmittal revision summary toggle works and updates content"
    - "Transmittal 'Open in Email Client' launches mailto link"
    - "New Project menu item opens confirmation dialog"
    - "New Project auto-saves current session and navigates to dashboard"
    - "Recent Projects list shows saved sessions with status badges"
  artifacts: []
  key_links:
    - from: "frontend/src/components/review/bottom-bar.tsx"
      to: "frontend/src/components/dialogs/transmittal-dialog.tsx"
      via: "Generate Transmittal button opens TransmittalDialog"
      pattern: "TransmittalDialog"
    - from: "frontend/src/components/dialogs/transmittal-dialog.tsx"
      to: "/api/transmittal/<id>"
      via: "GET request fetches email content"
      pattern: "getTransmittal"
    - from: "frontend/src/components/dialogs/new-project-dialog.tsx"
      to: "/api/session/<id>/save"
      via: "POST request saves session before reset"
      pattern: "saveSession"
---

<objective>
Human verification of transmittal and new project features in the browser.

Purpose: TRANS-01..04 and NEW-01..04 requirements have code committed but were never human-verified. This is a blocking prerequisite before marking these requirements complete in tracking documents.

Output: Verified pass/fail status for 8 requirements. If any fail, bugs must be fixed before proceeding to plan 08.1-03.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dialogs-finalization/06-03-SUMMARY.md
@.planning/phases/08.1-documentation-sync-verification/08.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev servers and prepare test state</name>
  <files></files>
  <action>
Start both Flask and Next.js dev servers using `npm run dev` from the project root (this runs both via concurrently). Wait for both to be ready (Flask on :5000, Next.js on :3000).

Then verify the app loads at http://localhost:3000 and that a test session exists with:
- At least one document uploaded and analyzed (risks visible in sidebar)
- At least one revision generated and accepted
- At least one paragraph flagged for client review

If no test session exists with these conditions, note this for the human verification step -- the user will need to create a session first or use an existing one.
  </action>
  <verify>Both servers respond: `curl -s http://localhost:5000/api/health` returns 200, and `curl -s http://localhost:3000` returns HTML content.</verify>
  <done>Dev servers running and test prerequisites documented for the human verification step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Transmittal email feature (TRANS-01..04) and New Project workflow (NEW-01..04) -- both built in Phase 6 Plan 03 but never human-verified.</what-built>
  <how-to-verify>
**Prerequisites:** Both servers must be running (npm run dev from project root). You need a session with at least one accepted revision and one flagged paragraph.

**Test 1: Transmittal (TRANS-01 through TRANS-04)**

1. Open a review session at http://localhost:3000/review/{sessionId}
2. Ensure at least one paragraph is flagged (click a paragraph, use the Flag button or sidebar flag action)
3. Click "Generate Transmittal" button in the bottom bar
4. TRANS-01: Verify the transmittal dialog opens showing email content with subject, greeting, and body
5. TRANS-02: Toggle the "Include revision summary" checkbox -- verify the content updates to show a "Key Revisions" section listing accepted revisions grouped by document section
6. TRANS-03: Verify the flagged items section shows each flag with its category label (Business Decision, Risk Alert, For Discussion, FYI) and any notes
7. TRANS-04: Click "Open in Email Client" -- verify your default email application opens with the transmittal content prefilled (subject and body). Note: on localhost, the mailto: link may be truncated for very long emails; the "Copy to Clipboard" button is the fallback.

**Test 2: New Project (NEW-01 through NEW-04)**

1. From the review page, click the hamburger menu (or user menu) in the header
2. NEW-01: Click "New Project" -- verify a confirmation dialog appears (note: per locked design decision, this auto-saves rather than asking save/discard)
3. NEW-02: Click "Continue" in the dialog -- verify the app auto-saves the current session (check /api/sessions endpoint to confirm session is saved)
4. NEW-03: After clicking Continue, verify you are redirected to the dashboard showing the intake form
5. NEW-04: On the dashboard, verify the "Recent Projects" section shows the session you just saved, with a status badge (Finalized, In Progress, or Not Started)

**Report format:** For each requirement (TRANS-01 through TRANS-04, NEW-01 through NEW-04), state PASS or FAIL with a brief note. If any FAIL, describe the issue.
  </how-to-verify>
  <resume-signal>Type the results for each requirement (PASS/FAIL) and any issues found, or type "all pass" if everything works. If any requirement FAILED, describe the bug -- the executor will fix it before proceeding to plan 08.1-03.</resume-signal>
</task>

</tasks>

<verification>
All 8 requirements (TRANS-01..04, NEW-01..04) verified by human tester in browser.
</verification>

<success_criteria>
- TRANS-01: Transmittal dialog opens with email content -- PASS
- TRANS-02: Revision summary toggle works -- PASS
- TRANS-03: Flagged items with categories appear -- PASS
- TRANS-04: Email client opens with content -- PASS
- NEW-01: New Project dialog appears -- PASS
- NEW-02: Auto-save works -- PASS
- NEW-03: UI returns to dashboard -- PASS
- NEW-04: Recent projects shows saved session -- PASS
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-documentation-sync-verification/08.1-01-SUMMARY.md`
</output>
